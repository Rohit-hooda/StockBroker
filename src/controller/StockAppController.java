package controller;

/**
 * Represents a controller of the stock application and is responsible to run and delegate tasks to
 * view and model.
 */
public interface StockAppController {

  /**
   * Runs the stock application ie, initialises, starts, runs and closes it.
   */
  void run();
}
